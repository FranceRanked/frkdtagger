{
    "schemaVersion": 1,
    "id": "${id}",
    "version": "${version}",
    "name": "${name}",
    "description": "${description}",
    "authors": [
        "Meek"
    ],
    "contact": {
        "sources": "${source}",
        "issues": "${issues}"
    },
    "license": "${license}",
    "icon": "assets/${id}/icon.png",
    "environment": "client",
    "entrypoints": {
        "client": [
            "com.meekdev.frkdtagger.FrkdTaggerClient"
        ]
    },
    "mixins": [
        "${id}.mixins.json"
    ],
    "depends": {
        "fabricloader": ">=0.18.4",
        "minecraft": "${mcdep}",
        "java": ">=${java_version}",
        "fabric-api": "*"
    }
}
